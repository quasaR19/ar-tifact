# Тестирование DetailsScreen

## GLB Viewer

### Базовая функциональность

#### Загрузка GLB модели из кэша

Модель загружается успешно

#### Загрузка GLB модели с сервера

Модель загружается успешно

#### Отображение индикатора загрузки

Проблема: "Модель загружена" не исчезает

#### Обработка ошибок загрузки

Ошибок загрузки пока не наблюдалось

### Управление камерой

#### Вращение камеры при drag (touch/mouse)

Проблема: слишком высокая скорость/чувствительность вращения камеры; при управлении камерой во viewer, происходит пролистывание страницы

#### Масштабирование при pinch-to-zoom (мобильные) или колесе мыши (ПК)

Проблема: слишком высокая скорость/чувствительность масштабирования камеры; при управлении камерой во viewer, происходит пролистывание страницы и поворот экрана

#### Кнопка "Сброс камеры" возвращает в начальную позицию

Пока сложно разобрать, работает ли как надо. Дальнейшие тестирование покажут

#### Кнопка "Центрировать" центрирует модель

Пока сложно разобрать, работает ли как надо. Дальнейшие тестирование покажут

#### Автоматическое центрирование при загрузке модели

Пока сложно разобрать, работает ли как надо. Дальнейшие тестирование покажут

#### GLB Viewer - Общее

Ситуация: открытый артефакт имеет две glb. Проблема: модели могут "залезать" в вьюпорты друг друга. Решение: одновременное отображение только одной модели во вьюпорте и ручное переключение с помощью выбора из списка/пролистывания.

Вьюпорт слишком мелкий, его следует растянут на всю ширину экрана

### Video Player

#### Загрузка видео из кэша

Проблема: видео не загружается.
"Ошибка: VideoPlayer cannot play url:file:///storage/emulated/...", "Cannot play file"

#### Загрузка видео с сервера

Проблема: видео не загружается.
Все также отображение того, что в "Загрузка видео из кэша". Нужно добавить более детальное логгирование для поиска проблемы

#### Video Player - Общее

Строка "Ошибка: VideoPlayer cannot play url:file:///storage/emulated/..." выходит за пределы экрана и добавляет ненужную горизонатльную прокрутку для страницы. Решение: добавить перенос текста

### Youtube Player

#### Парсинг различных форматов YouTube URL

Пропуск - все ссылки заведомо корректные

#### Загрузка thumbnail из YouTube

Загружается корректно

#### Отображение превью видео

Отображается корректно

#### Клик по кнопке Play открывает видео в браузере

Открывается в браузере корректно

#### Youtube Player - Общее

Для Youtube медиа следует убрать вывод метаданных - здесь они ничего не показывают

Треугольник в кнопке Play плохо выравнен по вертикали относительно круга - он немного ниже середины круга

После перехода, при возвращении из Youtube приложения с помощью "назад", происходит возврат в приложение, но затем сразу переход обратно в Youtube - следует сделать гарантию, что видео откроется только один раз, после клика

## Другое

Сделать растягивание превью изображения на всю ширину экрана

Сделать полосы прокрутки во всем приложении одинакового красивого стиля (Themes.uss)

Если контент страницы меньше высоты экрана, то контент можно пролистать выше и заставить его "зависнуть" в середине/в конце, он не возвращается в начало. При этом, если контент страницы больше высоты экрана, то появляется полоса прокрутки и ограничения на пролистывание работают нормально
